document.addEventListener('DOMContentLoaded',()=>{const form=document.getElementById('login-form');const message=document.getElementById('login-message');form.addEventListener('submit',async event=>{event.preventDefault();message.textContent='Ingresando...';const data=Object.fromEntries(new FormData(form).entries());try{const json=await apiPost('backend/auth/login.php',data);message.textContent=json.message;setTimeout(()=>location.href='products.html',700)}catch(error){message.textContent=error.message}});});