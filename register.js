document.addEventListener('DOMContentLoaded',()=>{const form=document.getElementById('register-form');const message=document.getElementById('register-message');form.addEventListener('submit',async event=>{event.preventDefault();message.textContent='Creando cuenta...';const data=Object.fromEntries(new FormData(form).entries());try{const json=await apiPost('backend/auth/register.php',data);message.textContent=json.message;setTimeout(()=>location.href='login.html',900)}catch(error){message.textContent=error.message}});});